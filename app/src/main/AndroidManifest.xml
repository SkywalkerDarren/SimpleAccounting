<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.skywalkerdarren.simpleaccounting">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name=".control.BillActivity"
            android:label="@string/title_activity_bill"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".control.BillListActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".control.BillPagerDetailActivity"
            android:label="@string/detail_bill" />

        <activity
            android:name=".control.BillEditActivity"
            android:windowSoftInputMode="stateVisible|adjustResize" />

    </application>

</manifest>