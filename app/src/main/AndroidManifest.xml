<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.skywalkerdarren.simpleaccounting">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.WelcomeActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/title_activity_bill"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.activity.BillDetailActivity"
            android:label="@string/detail_bill" />
        <activity
            android:name=".ui.activity.BillEditActivity"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity android:name=".ui.activity.StatsActivity" />

        <receiver android:name=".ui.DesktopWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bill_widget" />
        </receiver>

        <activity android:name=".ui.activity.SettingsActivity2" />
        <activity android:name=".ui.activity.AboutActivity">
            <intent-filter>
                <action android:name="simpleaccounting.about" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MyAccountActivity"></activity>
    </application>

</manifest>